# 贡献指南

请先区分要改的是数学、材料、评分判断还是程序。

数学纠错请给年份、小问、具体步骤、反例或完整推导。材料冲突请提供可公开的来源定位及前后版本；无法确认原文时保持待复核。评分异议请给主解路径、六维证据、T的错误发现与回退链，不只说数字偏高偏低。

程序修改先运行：

```bash
python -m unittest discover -s tests -v
python -m question_difficulty examples/minimal.json --output result.json
```

新增测试优先覆盖实际规则边界：半整数取整、并列依赖、机械重复、缺配分、冲突题、循环图和错误输入。不写仅复制实现的测试来增加数量。

如改变规则权重、阈值或路径解释，请创建新算法版本，不静默改现有规则。不因修改后某些题不再“看起来难”就倒推分数。

请勿提交个人文件路径、密钥、学生身份信息或未获得公开权限的整卷。使用新的来源文件时说明是否重复题；不要把同题不同转录当成独立样本。

---

版本号：v1.0
更新日期：2026-09-24
更新说明：明确纠错依据、测试要求、版本管理和材料公开约定。
